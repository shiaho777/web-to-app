# 暗亮色

[我的应用](/zh/guide/main-screen/my-apps)顶栏的月亮/太阳按钮切换整个构建器 UI 的深色模式。

## 工作方式

- 点击在 **深色** 与 **浅色** 之间切换。
- 切换时会从按钮处播放 circular **主题揭示** 动画,然后应用新主题。
- 你的选择会被 **持久化**,下次启动时恢复。

## 作用范围

这只控制 **构建器** 的外观。你生成的应用有自己的主题设置(亮/暗/跟随系统),在[应用配置](/zh/guide/config/appearance)中按应用配置。
